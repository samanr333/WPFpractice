<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <SolidColorBrush x:Key="myColor" Color = "{StaticResource appColor}" />
        <SolidColorBrush x:Key="myTextColor" Color="blue"></SolidColorBrush>
        <Color x:Key="textcolor">purple</Color>
        <Style x:Key="textStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="40"/>
            <Setter Property="Foreground" Value="{StaticResource myTextColor}"/>
        </Style>
    </Window.Resources>
    <Grid Background="{StaticResource myColor}">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <!--Grid.Row is attached property-->
        <TextBox Grid.Row="1" Grid.Column="1" Width="100" Height="20" x:Name="myName"></TextBox>
        <Button Grid.Column="1" Grid.Row="2" BorderBrush="Red" BorderThickness="3px" Click="showName">
            <!--Property element-->
            <Button.Content>Click me</Button.Content>
            <Button.Background>yellow</Button.Background>
            <Button.Height>30</Button.Height>
            <Button.Width>70</Button.Width>
        </Button>

        <TextBlock x:Name="result" FontWeight="Bold" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource textStyle}"></TextBlock>
    </Grid>
</Window>
